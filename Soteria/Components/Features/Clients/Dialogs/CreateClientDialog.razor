<MudDialog>
    <DialogContent>
        <MudForm Model="@Model" @ref="Form" Validation="@(ClientValidator.ValidateValue)">
            <MudStack Spacing="3">
                @if (!string.IsNullOrWhiteSpace(ErrorMessage))
                {
                    <MudAlert Severity="Severity.Error"
                              Variant="Variant.Outlined">
                        @ErrorMessage
                    </MudAlert>
                }

                <MudTextField @bind-Value="Model.ClientId"
                              Label="Client ID"
                              For="@(() => Model.ClientId)"
                              Immediate="true"
                              AutoFocus="true"
                              OnlyValidateIfDirty="true"
                              Disabled="IsSaving" />

                <MudTextField @bind-Value="Model.DisplayName"
                              Label="Display name"
                              For="@(() => Model.DisplayName)"
                              Immediate="true"
                              OnlyValidateIfDirty="true"
                              Disabled="IsSaving" />

                <MudTextField @bind-Value="Model.ClientSecret"
                              Label="Client secret"
                              For="@(() => Model.ClientSecret)"
                              Immediate="true"
                              OnlyValidateIfDirty="true"
                              Disabled="IsSaving" />

                <MudTextField @bind-Value="Model.ClientHost"
                              Label="Client host"
                              For="@(() => Model.ClientHost)"
                              Placeholder="https://example.com"
                              Immediate="false"
                              OnlyValidateIfDirty="true"
                              Disabled="IsSaving" />
            </MudStack>
        </MudForm>
    </DialogContent>

    <DialogActions>
        <MudButton OnClick="@Cancel"
                   Disabled="IsSaving">
            Cancel
        </MudButton>

        <MudButton Variant="Variant.Filled"
                   Color="Color.Primary"
                   OnClick="@SaveAsync"
                   Disabled="IsSaving">
            @if (IsSaving)
            {
                <MudProgressCircular Size="Size.Small"
                                     Indeterminate="true"
                                     Class="me-2" />
            }

            Save
        </MudButton>
    </DialogActions>
</MudDialog>